// Bài toán: xây dựng lớp đối tượng sao cho chỉ tồn tại nhiều nhất một đối tượng có kiểu dữ liệu là lớp này

// Bài toán mở rộng:
/*
    Áp dụng mẫu singleton để đóng gói các thuật toán sắp xếp các số thực kiểu float theo thứ tự tăng dần
    Các thuật toán:
    - Selection sort 
    - Insertion sort
    - Interchange sort 
    được đóng gói vào một lớp duy nhất tên là lớp SortAlgorithm
*/